# 과제·연구 리포트 스튜디오 사용 설명서

본 리포트 스튜디오는 무료 정적 호스팅(GitHub Pages)에 최적화된 **개인용 Markdown 기반 연구 리포트 관리 웹 애플리케이션**입니다. 별도의 서버나 데이터베이스 없이 사용자의 브라우저 내장 저장소(`LocalStorage`)를 활용하여 빠르고 안전하게 동작합니다.

---

## 1. 시작하기

### 1-1. 화면 구성
- **사이드바 (좌측)**: 전체 메뉴 (대시보드, 카테고리 관리, 리포트 목록, 에디터, 설정 및 백업) 및 `새 리포트 만들기` 버튼이 위치합니다.
- **메인 영역 (우측)**: 선택한 메뉴의 세부 내용을 조회하거나 편집하는 공간입니다.

### 1-2. 데이터 저장 주의사항
> **중요사항**: 작성하신 모든 내용은 브라우저 내부(`LocalStorage`)에 자동 저장됩니다. 브라우저 캐시 삭제 시 데이터가 유실될 수 있으므로, [4. 설정 및 백업] 기능을 활용해 주기적으로 **JSON 파일 내보내기**를 수행하시기를 권장합니다.

---

## 2. 기본 활용 가이드

### 1) 카테고리 설정하기
리포트를 작성하기 전, 문서들의 주제를 묶을 카테고리를 먼저 설정해 두면 편리합니다.
- 좌측 사이드바 `📁 카테고리 관리` 탭 혹은 `📊 대시보드` 하단에 위치한 **카테고리 목록**에 접속합니다.
- `빠른 카테고리 추가` 란에 이름(예: "딥러닝 스터디", "졸업 과제" 등)을 입력하고 `추가` 버튼을 누릅니다.
- 삭제된 카테고리의 리포트들은 안전하게 `미분류`로 이동됩니다.

### 2) 리포트 작성하기 (에디터)
- 사이드바 가장 상단의 `➕ 새 리포트 만들기` 버튼을 눌러 새 빈 문서를 엽니다.
- **에디터 뷰**에서 제목과 카테고리, 태그(쉼표 구분)를 기입합니다.

#### 프리셋(템플릿) 불러오기
내용 작성 전, 연구 기록을 수월하게 남길 수 있도록 **템플릿**을 제공합니다.
화면 중앙의 "템플릿 선택..." 드롭다운에서 `실험 보고서`, `문헌 리뷰`, `회의록` 중 하나를 선택하고 우측의 `적용` 버튼을 누릅니다.

#### Markdown 및 수식(LaTeX)
- 좌측 텍스트 에어리어에서 마크다운(Markdown) 문법을 사용할 수 있습니다. (`# 큰 제목`, `- 목록`, `**굵기**` 등)
- **수식(LaTeX) 지원**: 본문 곳곳에 아래 형식을 사용하면 우측 미리보기에 수식이 예쁘게 렌더링됩니다.
  - 인라인 수식: 달러 기호 한 개로 감쌉니다. (예: `$E=mc^2$`)
  - 블록 수식: 달러 기호 두 개로 위아래를 감쌉니다.
    ```md
    $$
    x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
    $$
    ```

### 3) 리포트 탐색 (리포트 목록)
- 좌측 메뉴 중 `🗂️ 리포트 목록`을 클릭합니다.
- 제목 또는 본문에 포함된 **키워드**를 검색창에 입력하면 관련 리포트가 즉시 필터링됩니다.
- 우측 상단의 카테고리 콤보박스를 통해 특정 카테고리 내의 리포트만 볼 수 있습니다.

---

## 3. 문서 외부 활용 및 인쇄

제출을 위해 리포트를 빼내거나 인쇄해야 할 때 유용한 방법입니다.

- **마크다운 내보내기 (에디터 뷰)**
  에디터 상단 `내보내기 (.md)` 버튼을 누르면 현재 리포트가 하나의 독립된 `.md` 파일로 다운로드됩니다. (단건 백업이나 GitHub 업로드를 할 때 유리합니다)
- **PDF 변환 및 인쇄 (에디터 뷰)**
  에디터 우측 중앙에 있는 `🖨️ PDF/인쇄` 버튼을 누르면, 브라우저 기본 인쇄창이 나타납니다. 사이드바와 입력 칸들은 모두 감춰지고 **미리보기 결과물만 온전히 출력/PDF 저장**할 수 있도록 최적화됩니다.

---

## 4. 백업 및 복원 (필수!)

데이터 안정성을 위해 `⚙️ 설정 및 백업` 메뉴를 자주 활용해주세요.

- **JSON 백업 내보내기**
  지금까지 작성한 "모든 카테고리와 리포트"를 하나의 `report-backup-날짜.json` 파일로 압축하여 다운로드합니다. 이 파일은 클라우드 드라이브 등에 안전하게 보관하세요.
- **데이터 복원 (JSON 파일 선택)**
  새로운 컴퓨터나 다른 브라우저에서 작업할 때, 백업해둔 JSON 파일을 눌러 가져오면 기존 환경을 완전히 덮어써 복구합니다.
