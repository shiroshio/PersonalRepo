# plus.md

## 목적
현재 리포트 스튜디오 MVP를 기준으로, 다음 개발 사이클에서 반영할 만한 개선 사항을 "추가할 기능"과 "수정/개선할 항목"으로 정리한다.

---

## 1. 추가할 만한 기능

### 1.1 에디터 생산성 향상
- [ ] 단축키 지원
  - `Ctrl+S`: 수동 저장
  - `Ctrl+B`: 굵게
  - `Ctrl+I`: 기울임
  - `Ctrl+/`: 수식 블록 삽입
- [ ] 실행 취소/다시 실행 버튼 (Undo/Redo)
- [ ] 드래그 앤 드롭 `.md` 파일 가져오기
- [ ] 자동 제목 제안 (본문 첫 줄 기반)

### 1.2 리포트 관리 기능
- [ ] 고정(Pin) 기능: 중요한 리포트 상단 고정
- [ ] 보관(Archive) 기능: 현재 목록에서 숨기고 별도 보관함으로 이동
- [ ] 중복 리포트 복제(Clone) 기능
- [ ] 태그별 필터 UI (현재는 텍스트 입력 위주)

### 1.3 백업/동기화 확장
- [ ] 백업 파일 자동 버전명 규칙 강화 (`report-backup-YYYYMMDD-HHmm.json`)
- [ ] 가져오기 시 병합(merge) 모드 제공
  - 현재: 덮어쓰기 중심
  - 확장: 기존 + 신규 병합, 충돌 시 선택
- [ ] GitHub Gist 연동(선택): 수동 백업 업로드/다운로드

### 1.4 연구 리포트 특화 기능
- [ ] 인용/참고문헌 블록 템플릿 추가 (APA/IEEE 스타일 가이드)
- [ ] Mermaid 다이어그램 지원
- [ ] 수식 스니펫 라이브러리
  - 자주 쓰는 선형대수, 확률/통계 수식 빠른 삽입

---

## 2. 수정/개선할 항목

### 2.1 안정성 및 예외 처리
- [ ] 템플릿 적용 전 자동 백업(임시 스냅샷) 옵션
- [ ] LocalStorage 용량 초과 에러 안내 UI
- [ ] JSON 복원 실패 시 상세 에러 패널 제공

### 2.2 UX 개선
- [ ] 카테고리/리포트 삭제 시 1회성 "되돌리기(Undo)" 토스트 제공
- [ ] 미리보기 닫힘 상태 기억 (새로고침 후 유지)
- [ ] 마지막으로 열었던 리포트 자동 복원
- [ ] 버튼 라벨/상태 메시지 일관성 점검

### 2.3 성능 개선
- [ ] 본문 입력 시 미리보기 렌더링 디바운스 분리
  - 저장 디바운스와 렌더링 디바운스를 분리해 체감 반응성 개선
- [ ] 긴 문서에서 KaTeX 렌더링 최적화
- [ ] 리포트 목록 가상 스크롤(문서 수 증가 대비)

### 2.4 구조 개선
- [ ] 현재 단일 `app.js`를 기능 단위로 재분리
  - `editor-ui.js`, `reports-ui.js`, `backup-ui.js` 등
- [ ] 공통 유틸 함수 정리 (`escapeHtml`, `downloadFile`, `safeFilename`)
- [ ] 테스트 가능한 순수 함수 분리

---

## 3. 우선순위 제안 (다음 스프린트)

### P1 (가장 먼저)
- 단축키 지원
- 마지막 리포트 자동 복원
- 미리보기 상태(열기/닫기) 유지
- LocalStorage 예외 처리 강화

### P2
- 고정/보관 기능
- JSON 병합 복원 모드
- 태그 필터 UI 고도화

### P3
- Mermaid, 인용 템플릿, 수식 스니펫 라이브러리
- 리포트 목록 가상 스크롤

---

## 4. 완료 기준 예시
- 단축키: 주요 4개 조합이 문서화되고 충돌 없이 동작
- 자동 복원: 새로고침 후 마지막 리포트 + 미리보기 상태 복원
- 복원 UX: 덮어쓰기/병합 모드 중 선택 가능, 실패 시 명확한 메시지 제공

---

## 5. 메모
현재 기능은 과제/연구용 개인 문서 관리 MVP로 충분히 동작한다. 다음 단계는 "데이터 안정성"과 "작성 생산성"을 우선 강화하고, 이후 연구 특화 기능을 확장하는 전략이 적합하다.
